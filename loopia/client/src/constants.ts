export { TILE_SIZE, PLAYER_SPEED, GAME_LOOP_INTERVAL } from '@shared/constants';

export const Constants = {
    // Network throttling (20 Hz instead of 60 FPS)
    MOVE_THROTTLE_MS: 50,  // 20 updates per second max

    // Chat
    CHAT_BUBBLE_DURATION: 5000,

    // Audio
    AMBIENT_VOLUME: 0.3
};
